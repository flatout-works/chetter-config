# syntax=docker/dockerfile:1.7

# Chetter Agent — Minimal Variant
# =========================================================================
# Lightweight task image with shared harnesses and common runtime tools, but no
# language-specific compiler/toolchain packages.
# =========================================================================

ARG BASE_IMAGE=ghcr.io/flatout-works/chetter-agent-base:main

FROM ${BASE_IMAGE}

WORKDIR /workspace
