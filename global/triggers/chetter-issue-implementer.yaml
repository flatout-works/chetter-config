# yaml-language-server: $schema=../../../chetter/schemas/trigger.schema.json
# Trigger: issue implementation
#
# When the approved label is added to an issue (by the triage agent), an
# agent reads the issue, plans the work, implements the solution, and
# opens a PR.
#
# Customize before enabling:
#   git_url      - repository URL to clone before working
#   git_ref      - branch to work from (usually main)
#   agent_image  - runner image (get from your Chetter admin or build your own)
#   provider_id  - LLM provider (synthetic for GLM, or your configured provider)
#   model_id     - model identifier (e.g. hf:zai-org/GLM-5.2)
#   timeout_sec  - adjust based on issue complexity (3600+ recommended)
#   skills       - skill hints for the runner (optional)
#
# To create: chetter_create_trigger with these fields.
# To change: chetter_update_trigger with changed fields.

name: chetter-issue-implementer
enabled: true
trigger_type: issue
repo: flatout-works/chetter
event: labeled
match_labels:
  - approved
agent_image: ghcr.io/flatout-works/chetter-agent:golang
harness: opencode
agent: issue-implementer
provider_id: deepseek
model_id: deepseek-v4-pro
git_url: https://github.com/flatout-works/chetter
git_ref: main
timeout_sec: 14400
prompt: |-
  You are implementing a solution for a GitHub issue that has been
  approved for implementation after triage. You have access to the
  repository and the `gh` CLI.

  ## Context

  Environment variables available to you:
  - ISSUE_NUMBER — the issue number
  - ISSUE_TITLE — the issue title
  - ISSUE_URL — the issue URL
  - ISSUE_BODY — the issue body
  - GITHUB_TOKEN — GitHub App installation token with issues/PRs read/write
  - GITHUB_REPO — repository (e.g., my-org/my-repo)

  ## Procedure

  ### 1. Understand the issue

  Read the issue description, triage assessment, and all comments:

  ```bash
  gh issue view $ISSUE_NUMBER --comments --json title,body,comments,labels
  ```

  Pay special attention to the triage comment — it contains the
  assessment, category, priority, and an implementation plan.

  ### 2. Plan the implementation

  Review the triage plan and inspect the relevant parts of the codebase.
  Identify which files, components, or modules need changes. Adjust the
  plan if you find issues the triage missed.

  ### 3. Implement

  Make minimal, focused changes. Follow existing code conventions and
  project structure. Run verification:

  ```bash
  make check
  ```

  If tests fail, fix them. Do not commit broken code. Make sure to add new
  tests that cover the new functionality.

   ### 4. Create a PR

   Create a branch named `fix/issue-ISSUE_NUMBER-short-description`,
   commit with a descriptive message referencing the issue, push, and
   open a pull request against `main`.

   The PR description must include:
   - **Closes**: `#ISSUE_NUMBER`
   - **Summary**: what was changed and why
   - **Checklist**: any verification steps you ran (make check, etc.)

  If the issue is already completed or a PR already exists, report that and exit without creating a new PR.
